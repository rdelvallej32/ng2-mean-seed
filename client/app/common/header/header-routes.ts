import { MenuType, RouteInfo } from './header.model';

export const ROUTES: RouteInfo[] = [
    { path: '', title: 'App Brand Here', menuType: MenuType.BRAND },
    { path: '', title: 'Sample Route', menuType: MenuType.LEFT }
]
